<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >
<!--
    tests级别：不同的test tag下的用例可以在不同的线程下执行
            相同的test tag下的用例只能在同一个线程中去执行
    classes级别：相同的class tag 下的用例在用一个线程中执行
                不同的class tag 下的用例可以在不同的线程中执行
    parallel 级别 methods 方法级别 ，thread-count 线程数
    methods级别：所有用例都可以在不同的线程下去执行
    thread-count: 代表了最大并发线程数
    xml 文件配置这种方式不能指定线程池，只能方法上才可以指定线程池
-->
<suite name="thread" parallel ="methods" thread-count="2">
    <test name ="demo1">
        <classes>
            <class name ="com.course.testng.multiThread.MultiThreadOnXml"/>
        </classes>
    </test>
    
    <test name="demo2">
        <classes>
            <class name="com.course.testng.multiThread.MultiThreadOnXml"></class>
        </classes>
        
    </test>

</suite>